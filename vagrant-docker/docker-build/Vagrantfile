Vagrant.configure("2") do |config|
  config.vm.provider "docker" do |d|
    d.build_dir = "."
    d.build_args = ["-t", "myubuntu:20.04"]
    d.name = "myubuntu-container"
    d.remains_running = true
  end
end
