Vagrant.configure("2") do |config|
  config.vm.provider "docker" do |d|
    d.build_dir = "."
    d.build_args = ["-t", "myubuntu:20.04"]
    # d.ports = ["8080:8080"]
    d.name = "myubuntu2"
    d.cmd = ["tail", "-f", "/dev/null"]
    d.remains_running = true
  end
end
